# Optional. Without it the app falls back to rule-based suggestions.
# Get a key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_key_here
# Optional override. Leave unset to auto-select a current model.
# GEMINI_MODEL=gemini-2.5-flash
