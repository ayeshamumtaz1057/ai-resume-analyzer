# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0

# Trained spaCy model. Optional — improves tokenisation slightly. Install with:
#   python -m spacy download en_core_web_sm
